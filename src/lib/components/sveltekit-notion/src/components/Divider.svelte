{#if false}
    <slot />
{/if}
<hr id={`_block-${block.id}`} class="notion-hr" />

<script>
    export let block = {}
    export let blocks = []; blocks;
    export let fullPage = false; fullPage;
    export let api = null; api;

</script>
